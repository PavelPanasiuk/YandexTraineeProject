pipeline {
    agent any      
    
    stages { 

            stage('Encoding') {
            steps
            {
               sh ''' echo 'chcp 65001' '''
            }

            }
    
            stage('Test') {
            steps 
            {
                 sh ''' 'dotnet test --filter Name~"SwitchLanguage"' ''''
            }
        }
        
    }
}