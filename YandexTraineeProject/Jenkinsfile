pipeline {
    agent any
     stages {
        stage('Test') {
            steps {
                echo 'dotnet test '
            }

    }

        stage('Reports') {
                steps([
                    includeProperties: false,
                    jdk: '',                    
                    reportBuildPolicy: 'ALWAYS',
                    results: [[path: '/YandexTraineeProject/allure-result']]
                ])
            }
    }
    
}

