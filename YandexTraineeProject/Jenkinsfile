pipeline {
    agent any      
    
    stages { 

            stage('Encoding') {
            steps
            {
               sh 'chcp 65001'
            }

            }
    
            stage('Test') {
            steps 
            {
                 sh 'dotnet test --filter Name~"SwitchLanguage"'
            }
        }
        
    }
}